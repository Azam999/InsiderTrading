import 'package:flutter/material.dart';

class Form10Q extends StatelessWidget {
  const Form10Q({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Form 10Q'),
      ),
    );
  }
}