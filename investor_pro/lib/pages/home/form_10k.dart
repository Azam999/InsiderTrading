import 'package:flutter/material.dart';

class Form10K extends StatelessWidget {
  const Form10K({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Form 10K'),
      ),
    );
  }
}